import React from 'react'

const XButton = (props) => (
  <span {...props} className="text-xs h-max_content text-red-700 py-1/2 px-1 cursor-pointer font-semibold rounded-full bg-gray-100 border border-solid border-black">X</span>
  )


export default XButton